package com.lovetropics.extras.block;

import net.minecraft.world.level.block.Block;

/**
 * LTExtras
 * FIRE EMOJI, FIRE EMOJI, FIRE EMOJI
 */
public class ShippingContainerBlock extends Block {

    public static class YellowShippingContainerBlock extends ShippingContainerBlock {
        public YellowShippingContainerBlock(Properties properties) {
            super(properties);
        };
    }

    public static class RedShippingContainerBlock extends ShippingContainerBlock {
        public RedShippingContainerBlock(Properties properties) {
            super(properties);
        };
    }

    public static class BlueShippingContainerBlock extends ShippingContainerBlock {
        public BlueShippingContainerBlock(Properties properties) {
            super(properties);
        };
    }

    public ShippingContainerBlock(Properties properties) {
        super(properties);
    };
}
